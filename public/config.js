// Configuration for API endpoints
const CONFIG = {
    // Používáme relativní cestu, takže funguje všude kde je server spuštěn
    // Frontend a backend jsou na stejné doméně
    API_URL: '/api'
};

// Export pro použití v admin.js
window.APP_CONFIG = CONFIG;
